apiVersion: v1
kind: Service
metadata: 
  name: svc-admin
  namespace: prod-app-shop
spec:
  selector:
    app: app-shop
    tier: admin-tools
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80