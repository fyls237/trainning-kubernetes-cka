apiVersion: v1
kind: Service
metadata:
  name: svc-front
  namespace: prod-app-shop
spec:
  selector:
    app: app-shop
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80