apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
  namespace: prod-database
spec:
  clusterIP: None
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: redis
    tier: database