apiVersion: batch/v1
kind: Job
metadata:
  name: simple-job
spec:
  template:
    spec:
      containers:
      - name: job
        image: busybox
        command: [ "echo", "Tache 1"]
      restartPolicy: Never